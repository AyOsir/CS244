#ifndef MEAN_H
#define MEAN_H

#include "Archive.h"

namespace labI
{
	float Mean(float data[], int size)
	{
	 for(int x = 0; x< a.size; x++
	     {
		total += a[x];
		}
		mean = total/a.size;
		cout << mean << "is the average" << endl; 
		return -1;
	}
}

#endif 
